export default function PägeTitle({ title }) {
  return (
    <div className="p-4">
      <h2 className="text-3xl font-[poppins] font-semibold text-gray-900 tracking-wide leading-tight">
        {title}
      </h2>
    </div>
  );
}
